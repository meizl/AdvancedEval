# AdvancedEval

一个 **Agent 全链路检测（Observability / Tracing）** 工具：追踪 agent 请求的完整执行链路，提供可视化界面查看请求的中间过程，并以「安装即用」的方式让其他 agent 项目快速接入。

## 核心目标

1. **全链路检测**：捕获 agent 请求从输入到输出的完整链路，包括推理/思考、工具调用、多步循环等中间环节。
2. **可视化中间过程**：通过 Web 界面查看每一步的中间状态，便于调试、排查与评测（eval）。
3. **安装即用**：其他 agent 项目安装本仓库即可使用，自动捕获链路，接入成本低、改造少。

## 已确定的技术决策

- **语言**：Python
- **目标 agent 框架**：框架无关 —— 通过统一协议/接口捕获链路，适配任意 agent（LangChain / LangGraph / OpenAI Agents SDK / 自研等）
- **数据存储**：可插拔（插件式存储后端，可替换：内存 / 本地文件 / 嵌入式 DB / 远端服务等）
- **交付形态**：本地起服务 + 网页面板

## 架构设计要点

分层结构：

```
捕获层（框架无关） → 统一 Trace/Span 数据模型 → 存储插件层（可插拔） → 本地服务（FastAPI）+ Web 面板
```

### 捕获层：两种方式协同（不冲突）

1. **自动 patch LLM SDK**（默认，开箱即用）：patch `openai` / `anthropic` 等客户端，无侵入捕获所有 LLM 调用与工具调用。
2. **显式 API**（补充）：装饰器 `@trace`、上下文管理器，用于标记自定义步骤、精细化控制。

两者通过 **contextvars 上下文传播** 协同：用 `contextvars.ContextVar` 维护「当前 span」，显式 API 进入时 push，自动 patch 触发时读取并挂载为子节点；无父级则自动创建根节点。

### 三个核心约束

1. **统一数据模型**：所有 span（显式/自动）结构一致，才能拼成完整链路。
2. **统一归属规则**：自动 span 默认挂到 contextvar 的当前 span 下；显式 API 负责创建/切换上下文。
3. **自动 patch 幂等、可叠加**：wrap 而非覆盖，保存原始方法引用，链式包裹，避免与用户框架自身的 patch 冲突。

## 当前状态

- 项目初始化阶段，尚未编写代码。
